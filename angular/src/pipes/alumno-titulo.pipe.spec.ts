import { AlumnoTituloPipe } from './alumno-titulo.pipe';

describe('AlumnoTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new AlumnoTituloPipe();
    expect(pipe).toBeTruthy();
  });
});
